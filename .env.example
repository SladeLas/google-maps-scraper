ENVIRONMENT=production
LOG_LEVEL=INFO
PORT=8001

# Optional: comma-separated origins for CORS (set to your ToolJet URL)
# Example: https://tooljet.mycompany.com,http://localhost:3000
ALLOWED_ORIGINS=

# Database connection (pick DB_URI or the individual pieces)
DB_URI=postgresql://postgres:postgres@postgres:5432/postgres
DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASSWORD=
